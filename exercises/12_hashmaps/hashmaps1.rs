// hashmaps1.rs
// 실행: cargo run --bin hashmaps1

use std::collections::HashMap;

fn main() {
    let mut map = HashMap::new();
    
    // TODO: "apple"을 키로, 3을 값으로 삽입하세요
    // 힌트: map.insert("apple", 3);
    
    println!("{:?}", map);
}
